-#  This file is app/views/movies/index.html.haml
%h1 All Movies

%table#movies
	%thead
		%tr
			-if @hilitedColumn == "title" 
				%th.hilite= link_to "Movie Title", movies_path({:OrderBy => "title"})
			-else
				%th= link_to "Movie Title", movies_path({:OrderBy => "title"})
			%th  Rating
			-if @hilitedColumn == "release_date"
				%th.hilite= link_to "Release Date", movies_path({:OrderBy => "release_date"})
			-else
				%th= link_to "Release Date", movies_path({:OrderBy => "release_date"})
			%th  More Info
	%tbody
		- @movies.each do |movie|
			%tr
				%td= movie.title 
				%td= movie.rating
				%td= movie.release_date
				%td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
